// operações aritimeticas

int a = 10;
int b = 5;
int soma = a + b; //Soma
int diferenca = a - b; // subtração
int produto = a * b; // multiplicação
int quociente = a / b; // divisão inteira
int resto = a % b;

// operadores de incremento e decremento
int contador = 0;
contador++; // incremento
contador--; // decremento

//operações de comparação
int x = 10;
int y = 5;
if(x > y) {
    //codigo se for verdadeiro ou falso    
}

//operações lógicas
int idade = 25;
if(idade >= 18 && idade <= 65) {
    // A pessoa é adulta
}

//operações de bits

int x = 5; // 101 em binario
int y = 3; // 011 em binario
int resultado = x & y; // 001 (1 em decimal) & (E bit a bit) | (OU bit a bit) ^(OU exclusivo bit a bit) <<(deslocamento à esquerda)

// conversão de tipos
int numeroInteiro = 42;
float numeroFlutuante = (float)numeroInteiro;

